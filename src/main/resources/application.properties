# Server
server.port=${PORT:8080}
server.forward-headers-strategy=framework

# Database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://viaduct.proxy.rlwy.net:18202/bootcamp_project_db?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:FePqyuIKNlxdnbMvaUSztRebLhiUhAUK}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Logging
logging.level.root=INFO
logging.level.com.bootcamp=DEBUG
logging.pattern.console=%d{HH:mm:ss} %-5level [%logger{20}] - %msg%n

# CORS
bootcamp.cors.allowed-origins=${BOOTCAMP_CORS_ALLOWED_ORIGINS:http://localhost:5173,https://localhost:5173,https://*.up.railway.app}

# Swagger/OpenAPI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

name=ganesh
